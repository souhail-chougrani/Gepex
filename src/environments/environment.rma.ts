export const environment = {
  production: true,
  useHash: true,
  version: require('../../package.json').version,
  buildDate: require('../../package.json').date,
  api: 'http://expertis/gepexwebapi/'
};
