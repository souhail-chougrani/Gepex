export const environment = {
  production: true,
  useHash: false,
  version: require('../../package.json').version,
  buildDate: require('../../package.json').date,
  api: 'https://gepexnet.ma/api/'
};
