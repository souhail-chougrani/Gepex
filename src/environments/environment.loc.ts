export const environment = {
  production: true,
  useHash: false,
  version: require('../../package.json').version,
  buildDate: require('../../package.json').date,
  api: 'http://192.168.0.101/gepex/api/'
};
